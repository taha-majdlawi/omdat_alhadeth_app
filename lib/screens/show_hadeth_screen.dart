import 'package:flutter/material.dart';

class ShowHadethScreen extends StatelessWidget {
  const ShowHadethScreen({super.key});
 
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Show Hadeth Screen'),
      ),
      body: Center(
        child: Text('This is the Show Hadeth Screen'),
      ),
    );
  }
}
