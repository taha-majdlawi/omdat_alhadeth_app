import 'package:flutter/material.dart';

class ShowBookDoors extends StatelessWidget {
  const ShowBookDoors({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}