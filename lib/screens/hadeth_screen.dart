import 'package:flutter/material.dart';

class HadethsScreen extends StatelessWidget {
  const HadethsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}